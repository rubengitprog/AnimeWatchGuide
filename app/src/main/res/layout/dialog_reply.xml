<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="?attr/colorBackgroundBlackAndWhite">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Write your reply:"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="?attr/colorTextBlackAndWhite"
        android:paddingBottom="8dp" />

    <EditText
        android:id="@+id/inputReplyText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="120dp"
        android:gravity="top|start"
        android:inputType="textMultiLine|textCapSentences"
        android:maxLength="500"
        android:hint="Write your reply..."
        android:textColor="?attr/colorTextBlackAndWhite"
        android:textColorHint="?attr/colorTextBlackAndWhite"
        android:alpha="0.8"
        android:padding="12dp"
        android:background="@android:drawable/edit_text"
        android:minLines="4"
        android:maxLines="10" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="4dp"
        android:text="Max 500 characters"
        android:textSize="11sp"
        android:textColor="?attr/colorTextBlackAndWhite"
        android:alpha="0.6" />

</LinearLayout>
